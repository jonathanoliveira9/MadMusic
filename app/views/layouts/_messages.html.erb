<% if flash.present? %>
  <script type="text/javascript">
    console.log("Here");
    <% flash.each do |f| %>
      <% type = f[0].to_s %>
      toastr['<%= type %>']('<%= f[1] %>');
  <% end %>
  
  </script>
<% end %>